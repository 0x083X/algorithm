/**
 * Leecode 707
 * 在单向链表类中实现这些功能
 * get(index) 获取链表第index个节点的值，如果索引无效 返回-1
 * addAtHead(val) 在链表的第一个元素之前添加一个值为val的节点
 * addAtTail(val) 将值为val的界定啊追加到链表的最后一个元素
 * addAtIndex(index,val) 在链表中的第index个节点之气那添加值为val的节点。如果 index 等于链表的长度，则该节点将附加到链表的末尾。如果 index 大于链表长度，则不会插入节点。如果index小于0，则在头部插入节点。
 * deleteAtIndex(index)：如果索引 index 有效，则删除链表中的第 index 个节点。
 */



/**
 * 1.get(index)
 */

